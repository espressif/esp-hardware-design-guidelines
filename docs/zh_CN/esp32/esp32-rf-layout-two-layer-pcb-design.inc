图 :ref:`fig-rf-layout-two-layer` 中高亮走线即为两层板设计中的射频走线。四层板设计不同的是，射频走线线宽会增加，通常为 20 mil 以上，确切数值需要根据阻抗计算公式进行调整，板层的变更也会影响到与阻抗有关的一些参数。

其他注意事项与四层板一致。

.. figure:: ../_static/{IDF_TARGET_PATH_NAME}/{IDF_TARGET_PATH_NAME}-pcb-rf-layout-two-layer.png
    :name: fig-rf-layout-two-layer
    :align: center
    :width: 70%
    :alt: {IDF_TARGET_NAME} 系列芯片两层板射频部分版图设计

    {IDF_TARGET_NAME} 系列芯片两层板射频部分版图设计