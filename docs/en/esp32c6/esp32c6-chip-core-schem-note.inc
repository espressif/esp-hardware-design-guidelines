.. note::
    - {IDF_TARGET_NAME} consists of variants in two packages, namely the QFN40 package and the QFN32 package. The main difference between these two packages is whether the flash is integrated into the chip's package.
    - Unless otherwise specified, "{IDF_TARGET_NAME}" used in this document refers to the QFN40 variant.
